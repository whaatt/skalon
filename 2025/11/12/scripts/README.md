# scripts

Scripts to preprocess and mask DEMs.

**Note:** Latitude bounds in `dem_sf_metadata.json` have been manually
perturbed by +0.0003 degrees each to better line up with the corresponding
GeoJSON.

**Note:** Latitude bounds in `dem_nyc_metadata.json` have been manually
perturbed by +0.002 degrees each to better line up with the corresponding
GeoJSON.
